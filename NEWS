See ChangeLog for more.

* 1.3.6

Gl_prompt_style changed from GL_LITERAL_PROMPT to GL_FORMAT_PROMPT (see
libtecla(3), optional prompt formatting).

configure checks for libmissing (FreeBSD only)
This is a FreeBSD extra library with missing math functions, added to
/usr/ports/math/libmissing (2012/12/20).

pop_forth_string: removed
file-jail: removed

Source code repository moved from cvs to svn on sf.net.
